using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Constants
{
    public static float maxUnitCubeLength = 1.75f;
    public static float maxNormalisedMagnitude = 1.75f; // sqrt(1^2 + 1^2 + 1^2) = swrt(3) = a bit less than 1.75
}
